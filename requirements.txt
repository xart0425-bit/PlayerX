# PlayerX 의존성 (Python 3.12) — 1~5단계
# 설치:  python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
#
# 이것만으로는 안 돌아간다. 네이티브 바이너리 두 개가 따로 필요하다:
#   vendor\libmpv-2.dll  — README 의 절차대로 직접 받는다
#   vendor\ffmpeg.exe    — python tools\fetch_ffmpeg.py  (자르기에만 필요)

PySide6==6.11.2
python-mpv==1.0.8

# 2단계: PTS 인덱싱과 무손실 캡쳐. FFmpeg 을 파이썬에서 직접 쓰는 바인딩이라
# 휠 안에 FFmpeg 이 통째로 들어 있다 (별도 설치 불필요). BSD-3 라 제약도 없다.
av==15.1.0

# 3단계 차분(diff) 계산과, 검증 도구의 PNG 픽셀 대조에 쓴다.
numpy==2.5.2

# 5단계 배포용. 앱을 돌리는 데는 필요 없다 — 묶을 때만 쓴다.
#   .venv\Scripts\pyinstaller PlayerX.spec
pyinstaller==6.22.2
